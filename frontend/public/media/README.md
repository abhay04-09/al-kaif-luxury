# AL-KAIF Media

Place the final brand film at:

```text
public/media/al-kaif-hero-film.mp4
```

The hero section already references this file and uses a cinematic image fallback when the video is not present.

The opening splash film lives at:

```text
public/media/al-kaif-splash.mp4
```

`components/motion/luxury-preloader.tsx` plays it full screen on the home page and
fades out when it ends.
