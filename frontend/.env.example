# AL-KAIFF backend (Cloudflare Worker). Defaults to the deployed Worker if unset.
NEXT_PUBLIC_API_URL="https://al-kaiff-api.adpatel8376.workers.dev"

# Auth (still using NextAuth for now — see notes in README)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-secret"
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Razorpay (checkout is not wired up yet)
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""

WHATSAPP_NUMBER="919876543210"
